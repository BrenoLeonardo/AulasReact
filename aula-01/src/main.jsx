import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
//ctrl+k+c para comentar 
//ctrl + z para voltar
//npm run dev para rodar em site web
//npm create vite@latest criar vite projeto
// npm install lembre-se dele logo após o npm create e no documento que será usado
// cd ~ retorna pastas anteriores